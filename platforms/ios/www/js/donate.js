function startDonate() {
    //set option to _blank for inapp or _system for safari app.
   console.log("starting donate");
   //var ref = window.open('http://thisiswhy.wesleyan.edu/waystogive', '_blank', 'location=yes');
   //ref.addEventListener('loadstart', function() { console.log('start: ' + event.url); });
   //ref.addEventListener('loadstop', function() { console.log('stop: ' + event.url); });
   //ref.addEventListener('exit', function() { ref.close(); });
   //ref.addEventListener('loaderror', function() { ref.close(); });
}
